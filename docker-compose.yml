version: '3'

services:
    db:
        image: postgres:12.3
        ports:
            - "5454:5432"
        restart: always
        environment:
            POSTGRES_PASSWORD: password
            POSTGRES_DB: tailgate
    smtp:
        image: mailhog/mailhog
        ports:
            - "1025:1025"
            - "8025:8025"
